# Copy this file to .env and fill in your values
# DO NOT commit .env to git

# Generate a random secret key with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_random_secret_key_here

# Flask settings
FLASK_DEBUG=False
PORT=5000
